services:
  - type: web
    name: sign-avatar-translator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --timeout 180 webapp.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
